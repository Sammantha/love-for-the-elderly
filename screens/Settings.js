import React, { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const Settings = props => {
	return (
		<View>
			<Text>This is the settings menu page</Text>
        </View>
	);
};

const styles = StyleSheet.create({

});

export default Settings;