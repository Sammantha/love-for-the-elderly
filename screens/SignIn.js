import React, { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const SignIn = props => {
	return (
		<View>
			<Text>The Sign In page</Text>
        </View>
	);
};

const styles = StyleSheet.create({

});

export default SignIn;